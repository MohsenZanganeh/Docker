version: "3"
services: 
  app:
    restart: always
    build: .
    container_name: node_test
    image: node_test
    environment:
      - "PORT=3800"
    ports:
      - "3800:3800"
  mongo:
    restart: always
    container_name: mongo
    image: mongo
# Defult docker compos create a network between services